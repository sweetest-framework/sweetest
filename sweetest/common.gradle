
compileKotlin {
    kotlinOptions {
        freeCompilerArgs += "-Xopt-in=kotlin.RequiresOptIn"
        freeCompilerArgs += "-Xopt-in=dev.sweetest.internal.InternalSweetestApi"
        freeCompilerArgs += "-Xopt-in=dev.sweetest.internal.CustomizableSweetestApi"
    }
}

compileTestKotlin {
    kotlinOptions {
        freeCompilerArgs += "-Xopt-in=kotlin.RequiresOptIn"
        freeCompilerArgs += "-Xopt-in=dev.sweetest.internal.InternalSweetestApi"
        freeCompilerArgs += "-Xopt-in=dev.sweetest.internal.CustomizableSweetestApi"
    }
}
